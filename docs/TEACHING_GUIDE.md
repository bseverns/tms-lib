# Teaching Guide
- Each header is heavily commented for classroom reading.
- Start with `core/Types.h` then open a DSP block (e.g., `dsp/TiltEQ.h`). 
- Use `control/Param.h` to demonstrate smoothing and zipper noise.
- Extend `dsp/LimiterLookahead.h` from clamp → envelope+lookahead as a lab.
